<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Bookshelf" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <item name="backgroundColor">@color/white</item>
        <!-- Customize your theme here. -->

        <item name="toolbarStyle">@style/MyActionBar</item>
    </style>

    <!--- ActionBar Style -->
    <style name="MyActionBar" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextColor">@color/black_700</item>
        <item name="android:colorPrimary">@color/black_700</item>
        <item name="android:background">@color/white</item>
        <item name="colorControlNormal">@color/black_700</item>
    </style>

    <style name="MyButon" parent="TextAppearance.AppCompat.Widget.Button">
        <item name="backgroundColor">@color/primary</item>
        <item name="android:textColor">@color/black_700</item>
    </style>

</resources>